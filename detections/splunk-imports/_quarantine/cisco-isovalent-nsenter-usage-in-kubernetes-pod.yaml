id: splunk-security-content-cd07120d-4265-481a-ba0f-3b91fbc5a02f
name: Cisco Isovalent - Nsenter Usage in Kubernetes Pod
description: 'This analytic detects the execution of the nsenter utility from within
  a container, a technique often used for exploitation and container escape. Nsenter
  allows an attacker to enter the namespaces of another process—such as the host''s
  init process (PID 1)—and execute a shell or other binaries with elevated privileges.
  For example, an attacker may use docker exec to gain a shell in a container, enumerate
  the PID of a target container or the host, and then use nsenter to access all namespaces
  (mount, UTS, IPC, net, pid) of the host or another container.  Example to escape
  to the host: `nsenter --target 1 --mount --uts --ipc --net --pid -- bash`. The WorkloadAncestorsBinary
  field is used to track the ancestry of the process, this is useful to understand
  the context of the nsenter usage.


  The options -m -u -n -i -p correspond to the various Linux namespaces. Adversaries
  exploit nsenter when pods are misconfigured with excessive privileges (e.g., privileged,
  hostPID, or broad hostPath mounts), enabling them to interact with the underlying
  node filesystem and processes. This can be an indicator of a container escape attempt
  or privilege escalation. Security teams should pay close attention to any nsenter
  invocation from within containers, especially outside of normal maintenance activity
  or in workloads with elevated privileges.'
severity: medium
enabled: false
tags:
  mitre:
  - T1543
  categories:
  - endpoint
references:
- https://isovalent.com/blog/post/2021-11-container-escape/
- https://kubehound.io/reference/attacks/CE_NSENTER/
logsource:
  product: splunk
detection:
  splunk_spl: "`cisco_isovalent_process_exec` process_name=\"nsenter\"\n| eval WorkloadAncestorsBinary=mvjoin(parent_process_name,\
    \ \" <- \")\n| stats count\n        min(_time) as firstTime\n        max(_time)\
    \ as lastTime\n        values(process) as process\n        values(WorkloadAncestorsBinary)\
    \ as WorkloadAncestorsBinary\n    by cluster_name container_id pod_name pod_namespace\
    \ pod_image_name parent_process_name process_name process_exec process_id node_name\n\
    | `security_content_ctime(firstTime)`\n| `security_content_ctime(lastTime)`\n\
    | `cisco_isovalent___nsenter_usage_in_kubernetes_pod_filter`"
provenance:
  source: splunk/security_content
  source_id: cd07120d-4265-481a-ba0f-3b91fbc5a02f
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/endpoint/cisco_isovalent___nsenter_usage_in_kubernetes_pod.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
