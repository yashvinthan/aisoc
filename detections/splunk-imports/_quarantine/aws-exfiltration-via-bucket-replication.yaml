id: splunk-security-content-eeb432d6-2212-43b6-9e89-fcd753f7da4c
name: AWS Exfiltration via Bucket Replication
description: The following analytic detects API calls to enable S3 bucket replication
  services. It leverages AWS CloudTrail logs to identify `PutBucketReplication` events,
  focusing on fields like `bucketName`, `ReplicationConfiguration.Rule.Destination.Bucket`,
  and user details. This activity is significant as it can indicate unauthorized data
  replication, potentially leading to data exfiltration. If confirmed malicious, attackers
  could replicate sensitive data to external accounts, leading to data breaches and
  compliance violations.
severity: medium
enabled: false
tags:
  mitre:
  - T1537
  categories:
  - cloud
references:
- https://hackingthe.cloud/aws/exploitation/s3-bucket-replication-exfiltration/
logsource:
  product: splunk
detection:
  splunk_spl: "`cloudtrail`  eventName = PutBucketReplication eventSource = s3.amazonaws.com\n\
    \  | rename user_name as user, requestParameters.ReplicationConfiguration.Rule.Destination.Bucket\
    \ as bucket_name\n  | stats count min(_time) as firstTime max(_time) as lastTime\n\
    \    BY signature dest user\n       user_agent src vendor_account\n       vendor_region\
    \ vendor_product bucket_name\n  | `security_content_ctime(firstTime)`\n  | `security_content_ctime(lastTime)`\n\
    \  | `aws_exfiltration_via_bucket_replication_filter`"
provenance:
  source: splunk/security_content
  source_id: eeb432d6-2212-43b6-9e89-fcd753f7da4c
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/aws_exfiltration_via_bucket_replication.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
