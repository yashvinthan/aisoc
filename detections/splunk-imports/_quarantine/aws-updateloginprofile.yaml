id: splunk-security-content-2a9b80d3-6a40-4115-11ad-212bf3d0d111
name: AWS UpdateLoginProfile
description: The following analytic detects an AWS CloudTrail event where a user with
  permissions updates the login profile of another user. It leverages CloudTrail logs
  to identify instances where the user making the change is different from the user
  whose profile is being updated. This activity is significant because it can indicate
  privilege escalation attempts, where an attacker uses a compromised account to gain
  higher privileges. If confirmed malicious, this could allow the attacker to escalate
  their privileges, potentially leading to unauthorized access and control over sensitive
  resources within the AWS environment.
severity: medium
enabled: false
tags:
  mitre:
  - T1136.003
  categories:
  - cloud
references:
- https://bishopfox.com/blog/privilege-escalation-in-aws
- https://rhinosecuritylabs.com/aws/aws-privilege-escalation-methods-mitigation-part-2/
logsource:
  product: splunk
detection:
  splunk_spl: "`cloudtrail` eventName = UpdateLoginProfile userAgent !=console.amazonaws.com\
    \ errorCode = success\n  | eval match=if(match(userIdentity.userName,requestParameters.userName),\
    \ 1,0)\n  | search match=0\n  | rename user_name as user\n  | stats count min(_time)\
    \ as firstTime max(_time) as lastTime\n    BY signature dest user\n       user_agent\
    \ src vendor_account\n       vendor_region vendor_product\n  | `security_content_ctime(firstTime)`\n\
    \  | `security_content_ctime(lastTime)`\n  | `aws_updateloginprofile_filter`"
provenance:
  source: splunk/security_content
  source_id: 2a9b80d3-6a40-4115-11ad-212bf3d0d111
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/aws_updateloginprofile.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
