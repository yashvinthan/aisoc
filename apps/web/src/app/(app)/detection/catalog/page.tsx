import { DetectionCatalog } from '@/components/detections/DetectionCatalog';

export const metadata = { title: 'Detection Catalog' };

export default function DetectionCatalogPage() {
  return <DetectionCatalog />;
}
