id: splunk-security-content-2a9b80d3-6340-4345-b5ad-290bf3d222c4
name: AWS EC2 Snapshot Shared Externally
description: The following analytic detects when an EC2 snapshot is shared with an
  external AWS account by analyzing AWS CloudTrail events. This detection method leverages
  CloudTrail logs to identify modifications in snapshot permissions, specifically
  when the snapshot is shared outside the originating AWS account. This activity is
  significant as it may indicate an attempt to exfiltrate sensitive data stored in
  the snapshot. If confirmed malicious, an attacker could gain unauthorized access
  to the snapshot's data, potentially leading to data breaches or further exploitation
  of the compromised information.
severity: medium
enabled: false
tags:
  mitre:
  - T1537
  categories:
  - cloud
references:
- https://labs.nettitude.com/blog/how-to-exfiltrate-aws-ec2-data/
- https://stratus-red-team.cloud/attack-techniques/AWS/aws.exfiltration.ec2-share-ebs-snapshot/
- https://hackingthe.cloud/aws/enumeration/loot_public_ebs_snapshots/
logsource:
  product: splunk
detection:
  splunk_spl: "`cloudtrail` eventName=ModifySnapshotAttribute\n  | rename requestParameters.createVolumePermission.add.items{}.userId\
    \ as requested_account_id\n  | search requested_account_id != NULL\n  | eval match=if(requested_account_id==aws_account_id,\"\
    Match\",\"No Match\")\n  | where match = \"No Match\"\n  | rename user_name as\
    \ user\n  | stats count min(_time) as firstTime max(_time) as lastTime\n    BY\
    \ signature dest user\n       user_agent src vendor_account\n       vendor_region\
    \ vendor_product requested_account_id\n  | `security_content_ctime(firstTime)`\n\
    \  | `security_content_ctime(lastTime)`\n  | `aws_ec2_snapshot_shared_externally_filter`"
provenance:
  source: splunk/security_content
  source_id: 2a9b80d3-6340-4345-b5ad-290bf3d222c4
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/aws_ec2_snapshot_shared_externally.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
