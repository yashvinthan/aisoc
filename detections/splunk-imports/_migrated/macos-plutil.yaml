id: splunk-security-content-c11f2b57-92c1-4cd2-b46c-064eafb833ac
name: MacOS plutil
description: The following analytic detects the usage of the `plutil` command to modify
  plist files on macOS systems. It leverages osquery to monitor process events, specifically
  looking for executions of `/usr/bin/plutil`. This activity is significant because
  adversaries can use `plutil` to alter plist files, potentially adding malicious
  binaries or command-line arguments that execute upon user logon or system startup.
  If confirmed malicious, this could allow attackers to achieve persistence, execute
  arbitrary code, or escalate privileges, posing a significant threat to the system's
  security.
severity: medium
enabled: false
tags:
  mitre:
  - T1647
  categories:
  - endpoint
references:
- https://osquery.readthedocs.io/en/stable/deployment/process-auditing/
logsource:
  product: splunk
detection:
  splunk_spl: "`osquery_macro` name=es_process_events columns.path=/usr/bin/plutil\n\
    \  | rename columns.* as *\n  | stats count  min(_time) as firstTime max(_time)\
    \ as lastTime\n    BY username host cmdline\n       pid path parent\n       signing_id\n\
    \  | rename username as user, cmdline as process, path as process_path, host as\
    \ dest\n  | `security_content_ctime(firstTime)`\n  | `security_content_ctime(lastTime)`\n\
    \  | `macos_plutil_filter`"
provenance:
  source: splunk/security_content
  source_id: c11f2b57-92c1-4cd2-b46c-064eafb833ac
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/endpoint/macos_plutil.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
