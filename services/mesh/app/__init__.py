"""AiSOC Federated Threat Intel Mesh (v8 P1).

Opt-in gossip of two privacy-preserving artifact types between self-hosted AiSOC
instances via a lightweight hub: hashed IOC sightings and verdict signatures.
Every install can make every other install smarter — the one feature class a
closed SaaS vendor structurally cannot copy credibly, because it requires users
to trust the code.
"""
