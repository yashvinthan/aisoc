"""Repo-level tools (detection importers, dataset generators, etc.).

Each subpackage is independent. Imports assume the repo root is on
``sys.path`` (true when invoked via ``python3 -m tools.<subpackage>.<module>``
from the repo root).
"""
