id: splunk-security-content-886a8f46-d7e2-4439-b9ba-aec238e31732
name: ASL AWS ECR Container Upload Unknown User
description: The following analytic detects unauthorized container uploads to AWS
  Elastic Container Service (ECR) by monitoring AWS CloudTrail events. It identifies
  instances where a new container is uploaded by a user not previously recognized
  as authorized. This detection is crucial for a SOC as it can indicate a potential
  compromise or misuse of AWS ECR, which could lead to unauthorized access to sensitive
  data or the deployment of malicious containers. By identifying and investigating
  these events, organizations can mitigate the risk of data breaches or other security
  incidents resulting from unauthorized container uploads. The impact of such an attack
  could be significant, compromising the integrity and security of the organization's
  cloud environment.
severity: medium
enabled: false
tags:
  mitre:
  - T1204.003
  categories:
  - cloud
references:
- https://attack.mitre.org/techniques/T1204/003/
logsource:
  product: splunk
detection:
  splunk_spl: "`amazon_security_lake` api.operation=PutImage NOT `aws_ecr_users_asl`\n\
    \  | fillnull\n  | stats count min(_time) as firstTime max(_time) as lastTime\n\
    \    BY actor.user.uid api.operation api.service.name\n       http_request.user_agent\
    \ src_endpoint.ip actor.user.account.uid\n       cloud.provider cloud.region\n\
    \  | rename actor.user.uid as user api.operation as action api.service.name as\
    \ dest http_request.user_agent as user_agent src_endpoint.ip as src actor.user.account.uid\
    \ as vendor_account cloud.provider as vendor_product cloud.region as vendor_region\n\
    \  | `security_content_ctime(firstTime)`\n  | `security_content_ctime(lastTime)`\n\
    \  | `asl_aws_ecr_container_upload_unknown_user_filter`"
provenance:
  source: splunk/security_content
  source_id: 886a8f46-d7e2-4439-b9ba-aec238e31732
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/asl_aws_ecr_container_upload_unknown_user.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
