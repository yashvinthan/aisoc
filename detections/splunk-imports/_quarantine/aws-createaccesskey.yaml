id: splunk-security-content-2a9b80d3-6340-4345-11ad-212bf3d0d111
name: AWS CreateAccessKey
description: The following analytic identifies the creation of AWS IAM access keys
  by a user for another user, which can indicate privilege escalation. It leverages
  AWS CloudTrail logs to detect instances where the user creating the access key is
  different from the user for whom the key is created. This activity is significant
  because unauthorized access key creation can allow attackers to establish persistence
  or exfiltrate data via AWS APIs. If confirmed malicious, this could lead to unauthorized
  access to AWS services, data exfiltration, and long-term persistence in the environment.
severity: medium
enabled: false
tags:
  mitre:
  - T1136.003
  categories:
  - cloud
references:
- https://bishopfox.com/blog/privilege-escalation-in-aws
- https://rhinosecuritylabs.com/aws/aws-privilege-escalation-methods-mitigation-part-2/
logsource:
  product: splunk
detection:
  splunk_spl: "`cloudtrail` eventName = CreateAccessKey userAgent !=console.amazonaws.com\
    \ errorCode = success\n  | eval match=if(match(userIdentity.userName,requestParameters.userName),1,0)\n\
    \  | search match=0\n  | rename user_name as user\n  | stats count min(_time)\
    \ as firstTime max(_time) as lastTime\n    BY signature dest user\n       user_agent\
    \ src vendor_account\n       vendor_region vendor_product\n  | `security_content_ctime(firstTime)`\n\
    \  | `security_content_ctime(lastTime)`\n  | `aws_createaccesskey_filter`"
provenance:
  source: splunk/security_content
  source_id: 2a9b80d3-6340-4345-11ad-212bf3d0d111
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/aws_createaccesskey.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
