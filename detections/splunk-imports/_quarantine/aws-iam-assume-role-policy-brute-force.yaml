id: splunk-security-content-f19e09b0-9308-11eb-b7ec-acde48001122
name: AWS IAM Assume Role Policy Brute Force
description: The following analytic detects multiple failed attempts to assume an
  AWS IAM role, indicating a potential brute force attack. It leverages AWS CloudTrail
  logs to identify `MalformedPolicyDocumentException` errors with a status of `failure`
  and filters out legitimate AWS services. This activity is significant as repeated
  failures to assume roles can indicate an adversary attempting to guess role names,
  which is a precursor to unauthorized access. If confirmed malicious, this could
  lead to unauthorized access to AWS resources, potentially compromising sensitive
  data and services.
severity: medium
enabled: false
tags:
  mitre:
  - T1580
  - T1110
  categories:
  - cloud
references:
- https://www.praetorian.com/blog/aws-iam-assume-role-vulnerabilities/
- https://rhinosecuritylabs.com/aws/assume-worst-aws-assume-role-enumeration/
- https://www.elastic.co/guide/en/security/current/aws-iam-brute-force-of-assume-role-policy.html
logsource:
  product: splunk
detection:
  splunk_spl: "`cloudtrail` (errorCode=MalformedPolicyDocumentException) status=failure\
    \ (userAgent!=*.amazonaws.com)\n  | rename user_name as user\n  | stats count\
    \ min(_time) as firstTime max(_time) as lastTime values(requestParameters.policyName)\
    \ as policy_name\n    BY src, user, vendor_account\n       vendor_region, vendor_product,\
    \ signature,\n       dest, errorCode\n  | where count >= 2\n  | `security_content_ctime(firstTime)`\n\
    \  | `security_content_ctime(lastTime)`\n  | `aws_iam_assume_role_policy_brute_force_filter`"
provenance:
  source: splunk/security_content
  source_id: f19e09b0-9308-11eb-b7ec-acde48001122
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/cloud/aws_iam_assume_role_policy_brute_force.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
