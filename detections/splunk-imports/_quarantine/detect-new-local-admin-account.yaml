id: splunk-security-content-b25f6f62-0712-43c1-b203-083231ffd97d
name: Detect New Local Admin account
description: The following analytic detects the creation of new accounts elevated
  to local administrators. It uses Windows event logs, specifically EventCode 4720
  (user account creation) and EventCode 4732 (user added to Administrators group).
  This activity is significant as it indicates potential unauthorized privilege escalation,
  which is critical for SOC monitoring. If confirmed malicious, this could allow attackers
  to gain administrative access, leading to unauthorized data access, system modifications,
  and disruption of services. Immediate investigation is required to mitigate risks
  and prevent further unauthorized actions.
severity: medium
enabled: false
tags:
  mitre:
  - T1136.001
  categories:
  - endpoint
references: []
logsource:
  product: splunk
detection:
  splunk_spl: "`wineventlog_security`\n(\n  EventCode=4720\n  OR\n  (\n    EventCode=4732\n\
    \    AND\n    (\n      Group_Name=Administrators\n      OR\n      TargetUserName=Administrators\n\
    \    )\n  )\n)\n| transaction user dest connected=false maxspan=180m\n| stats\
    \ count min(_time) as firstTime\n              max(_time) as lastTime\n      \
    \        dc(EventCode) as distinct_eventcodes\n  by src_user user dest\n| where\
    \ distinct_eventcodes > 1\n| `security_content_ctime(firstTime)`\n| `security_content_ctime(lastTime)`\n\
    | `detect_new_local_admin_account_filter`"
provenance:
  source: splunk/security_content
  source_id: b25f6f62-0712-43c1-b203-083231ffd97d
  source_commit: 4d4c7ee
  license: Apache-2.0
  license_url: https://github.com/splunk/security_content/blob/develop/LICENSE
  imported_at: '2026-05-04'
  imported_by: splunk_importer
  upstream_path: detections/endpoint/detect_new_local_admin_account.yml
notes:
  splunk_status: production
  splunk_data_source: []
  quarantine_reason: raw SPL — needs translation to AiSOC detection schema
